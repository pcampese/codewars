def isValidWalk(walk):
	# Display arguments
	print('walk = {}'.format(walk))
	
	# First check to make sure that there are exactly 10 directions provided
	if (len(walk) != 10):
		result = False
	else:
		return True

	return result